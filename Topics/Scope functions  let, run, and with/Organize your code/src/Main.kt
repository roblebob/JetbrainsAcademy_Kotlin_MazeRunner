fun main() {
    val data = run {
        val day = readln()
        val month = readln()
        val year = readln()
        "$month.$day.$year"
    }

    // Do not change the code below 
    println(data)
}